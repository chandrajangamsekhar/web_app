<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: {class: "form-signin"}) do |f| %>
  <h2 class="form-signin-heading"><%= t "devise.invitations.edit.header" %></h2>
  <h2 class="form-signin-logo"><img src="/assets/logo.png"></h2>
  <div class="login-wrap">
    <% resource.class.invite_key_fields.each do |field| -%>
      <div class="form-inputs">
       <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
      </div>
    <% end -%>
    <div class="form-actions">
      <%= f.button :submit, "Reset Password",class: "btn btn-lg btn-info btn-block" %>
    </div>
    <%= render "users/shared/links" %>
  </div>
<% end %>